import { GetQuizByIdPipe } from './get-quiz-by-id.pipe';

describe('GetQuizByIdPipe', () => {
  it('create an instance', () => {
    const pipe = new GetQuizByIdPipe();
    expect(pipe).toBeTruthy();
  });
});
